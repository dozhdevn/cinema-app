import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  navItem: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 4,
  },
  text: {
    marginLeft: 8,
    fontWeight: '500',
    fontSize: 20,
  },
})

export default styles
