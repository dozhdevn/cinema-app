import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  movieTitle: {
    color: '#ffffff',
    paddingHorizontal: 8,
    marginBottom: 2,
    fontSize: 24,
    fontWeight: '600',
  },
})

export default styles
